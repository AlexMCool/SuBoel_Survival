##############################################################################
#                         factory player calculation                         #
##############################################################################
factory_player_calculation:
  command: /factory_player_calculation
  type: RUN_AS_OPERATOR
  register: true
  runcmd:
  #######################################
  #            id available             #
  #######################################
  # arg1 = $s.factory.calculation
  # arg2 = $player
  # arg3 = $s.factory.init.player_factory_id
  - '$Script$%if%$PlayerDataFor%$arg2%$s.factory.factory_id.$arg3.id%==NoData'
  
  # set player's player_factory_id to corresponding factory id
  - '$Script$%PlayerDataFor%$arg2%$s.factory.factory_id.$arg3.id=$arg1'
  
  #######################################
  #           id unavailable            #
  #######################################
  # arg1 = $s.factory.calculation
  # arg2 = $player
  # arg3 = $s.factory.init.player_factory_id
  - '$Script$%else%'
  
  # add '1' to calculation
  - '$Script$%Variable%$s.factory.init.player_factory_id+1'
  
  # recur
  - '/mycommand:factory_player_calculation $s.factory.calculation $player $s.factory.init.player_factory_id $PlayerData%$s.factory.factory_id.$arg3.id'
